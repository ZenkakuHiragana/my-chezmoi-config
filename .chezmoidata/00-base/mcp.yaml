# ハーネス共通の MCP 定義。
# 環境固有の追加は .chezmoidata/99-specific/mcp.yaml に置く。
mcpServers:
  exa:
    url: https://mcp.exa.ai/mcp
  knowledge-finder:
    command: node
    args:
      - '{file:~/.config/opencode/knowledge-finder-entrypoint.txt}'
    timeout: 10000
    enabled: true
