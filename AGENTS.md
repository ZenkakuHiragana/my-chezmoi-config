## 適用範囲と責務境界

- このファイルの規則は、このリポジトリを保守する編集者へ適用する。生成した環境で実行者が従う規則は、生成物の正本に置く。
- 規則は、適用主体と実行時期を保持して扱う。リポジトリ保守、成果物の記述規則、生成物の責務配置と実行時統治を区別する。
- 内容が関連しているだけで、一つの区分の規則を別の区分へ転記、一般化、埋め込みしてはならない。
- スキルを管理するための規則を、スキルを利用する実行者自身の規則へ一般化してはならない。生成物の実行時規則を、リポジトリを保守する編集者の規則や他の成果物へ伝播させてはならない。
- 規則を別の層へ置くときは、その層の適用主体と実行時期に対して一般に成立する根拠を示す。リポジトリ固有の運用だけを理由に、別の層へ移してはならない。

## リポジトリ保守

### プロンプト運用に使うファイル

- プロンプト運用の正本と運用テンプレートはリポジトリ内に分離して管理しています。
  - プロンプト運用原則: opencode-prompt-dev/prompt-principles.md
  - 失敗記録:
    - 標準のローカル失敗ログ保存先: `~/.local/share/chezmoi/.opencode/local-failure-logs/`
    - 失敗記録手順: `dot_config/opencode/agents/report-failure.md`
    - セッション採掘手順: `dot_config/opencode/agents/extract-failure-patterns.md`
    - 失敗の選別と介入判断手順: `dot_config/opencode/agents/triage-failure.md`
    - リファクタリングチェックリスト等: opencode-prompt-dev/prompt-refactor-checklist.md
- 後述のプロンプト階層に属する面を追加、変更、削除する場合は、作業経路、作業規模、変更理由にかかわらず、編集前と完了前に `opencode-prompt-dev/prompt-refactor-checklist.md` を検査基準として使う。適用するかどうかを実行者自身の必要性判断へ委ねてはならない。
- 失敗ログ、セッション採掘レポート、失敗の選別と介入判断レポートはローカル失敗ログルートへ保存する。追跡対象ファイルには運用規則と確定済みの改善だけを残す。
- 失敗、または失敗対応として行う改善案は、上記の失敗ログに記録し、現行カバレッジ確認とトリアージを経由する。失敗対応でない能力追加やその他の変更へ、この経路を要求してはならない。介入が決まったときは、リファクタリングまたは選択した修正手続きで差分を適用する。

### プロンプト設計・改良における外部調査

このリポジトリでプロンプトの追加、改良、失敗分析、階層整理を行うとき、外部の指針や現在の実務が品質に実質的な影響を与えうる場合は、ローカルのプロンプト群だけを根拠に確定してはならない。

プロンプトや作業手順の規則を「変えるべきか判断する」依頼は、次にリポジトリ変更へ進む前提が明示される場合のみ、リポジトリ変更意図を含むものとして扱う。
相談・助言のみで即時の実装移行を要求しない場合は、純粋な方針相談として扱う。
ただし、実装前の判断でも明確に変更判断が要求されている場合は、外部調査中も `context-clarification` を能力集合に残す。

特に次のいずれかに当てはまる場合は、関連コマンド側で外部調査の要否を判定すること。

- 現在の最良慣行が設計を実質的に左右しうる
- 情報源方針、根拠基準、プライバシー、セキュリティ、検証戦略を変更する
- 用語、手法、規範が曖昧・未確定・変化中である
- `claim-grounding`、`refactoring`、`technical-writing`、確認手順など、分野固有の実務慣行が品質に影響する

外部事実の確認が必要な場合は、主張と許可された公開根拠を固定して `claim-grounding` を用いること。

ここでは「いつ調べるべきか」という原則だけを定める。
根拠接地の手順そのものは `claim-grounding` に置き、実際の判定と反映は `/add-capability` や `/refine-prompt` などのコマンド側で行う。

## 成果物の記述規則

### プロンプトに使用する言語

- エージェントのシステムプロンプトとコマンド用プロンプトは次の方針で書く。
  - フロントマターの `description`: 日本語で、使う条件と使わない条件と得られるものを短く書く。外部コードと一致する名前（スキル名、エージェント名、コマンド名）だけ英語のまま維持する。詳細手順は書かない。
  - 本文: 日本語で短く書く。逐語翻訳ではなく、役割、条件、手順、禁止事項、出力、完了条件を箇条書き中心に再設計する。
  - 機械的に照合される値、キー、固定名だけ英語のまま維持する。例: エージェント名、スキル名、経路制御名、スキーマフィールド、`status` 値、`severity` 値、引き渡し名、`task_kind`。作業票の項目名と値は日本語（`作業分類`、`実行モード`、`単発`、`読み取り専用` など）を使う。
  - 制御語彙の棚卸しは `opencode-prompt-dev/control-vocabulary.md` を正本にする。
  - 制御語彙以外の英語は避ける。残す場合は、本文の近くで意味が分かるように書く。
  - 出力スキーマ、フロントマターのキー、テンプレート取り込み名、コマンド名は既存の英語を維持する。
  - 長い理由説明はエージェントプロンプト本体に置かず、運用メモ、レポート、参考資料に分離する。
  - プロンプト本体の日本語は丁寧語を避け、仕様書的な短文を優先する。
  - プロンプト本文では、規則や手順をファイル名で参照してはならない。文脈に積まれた指示がどのファイルから来たかは、ハーネスの自動注入（AGENTS.md 等の自動読み込み）に依存し、一般には保証されないため、実行エージェントはファイル名が指す内容を特定できない。参照する場合は層（共通規則、親エージェント用テンプレート、サブエージェント、スキル）または概念名で参照する。パス、例示、制御語彙（スキル名、エージェント名、コマンド名）は例外。
- 対象ファイル:
  - ./.opencode/commands/\*.md
  - ./dot_config/opencode/agents/\*.md
  - ./dot_config/opencode/agents/\*.md.tmpl
  - ./dot_config/opencode/commands/\*.md
  - ./.chezmoitemplates/opencode/AGENTS.md
  - ./.chezmoitemplates/opencode/agents/\*.md
  - ./.chezmoitemplates/opencode/parent/\*.md
  - ./.chezmoitemplates/prompt/\*.md
- スキル定義は次の方針で書く。
  - `name`: 英語の kebab-case（ハイフン区切り小文字）を維持する。
  - フロントマターの `description`: 日本語で、使う条件と使わない条件と得られるものを短く書く。外部コードと一致する名前（スキル名、エージェント名、コマンド名）だけ英語のまま維持する。詳細手順は書かない。
  - 本文: 日本語で短く書く。逐語翻訳ではなく、用途、手順、出力、チェックを箇条書き中心に再設計する。
  - スキルは原則として独立能力として扱い、その `SKILL.md` だけで目的、入力、判断、対象への働きかけ、停止条件、結果に必要な情報を説明・実行できるようにする。ホストの運用プロトコル、統治手順、他スキルの接続、呼び出し側の固定形式へ依存させてはならない。複数能力の接続、使用順序、委譲、採否そのものが明示的な責務であるオーケストレーションだけを例外とする。例外に該当しないスキルへ、能力の組み合わせ、委譲、作業段階、採否を書いてはならない。
  - 機械的に照合される値、キー、固定名だけ英語のまま維持する。
  - 制御語彙の棚卸しは `opencode-prompt-dev/control-vocabulary.md` を正本にする。
  - 制御語彙以外の英語は避ける。残す場合は、本文の近くで意味が分かるように書く。
  - コマンドスキルテンプレートの本文が `dot_config/opencode/agents/*.md` を取り込む場合、取り込み先の言語規則に従う。
  - 規則や手順をファイル名で参照してはならない。層または概念名で参照する。パス、例示、制御語彙は例外。
- 失敗事例、プロンプト運用規則など、プロンプトを改良するための作業手順に関する記載は日本語で書く。具体的には以下のファイルが対象。
  - ./opencode-prompt-dev/\*.md
  - ./AGENTS.md

### 義務語彙の統制

- 義務語彙の正本は `opencode-prompt-dev/obligation-vocabulary.md` とする。
- 義務を述べる指示文は、正本にある正規マーカーを使う。
- 要求する挙動は必須の正規マーカーで書く。
- 禁止する挙動は禁止の正規マーカーで書く。
- 推奨、非推奨、任意は意識的に選択する。
- RFC 2119 風の英字マーカーは分類の参考名として扱い、プロンプト本文の義務表現には使わない。
- 要求する挙動を任意の正規マーカーや曖昧表現で書いてはならない。
- どの義務レベルにも属さない曖昧表現は、指示が無効化されたものとして扱う。
- 曖昧表現を見つけたら、義務レベルへ割り当てるか削除する。
- 曖昧表現を残すときは、義務レベルを持たない説明文であることを文脈から判定できる形にする。

### 用語集

- 用語の正確な定義と表記揺れの抑止は `opencode-prompt-dev/japanese-glossary.md` を正本とする。プロンプトの編集時は用語集を参照する。
- 用語集の定義をプロンプト本文へ複製してはならない。定義を添えなければ伝わらない語で概念を表すことは命名の失敗であり、定義の複製で補ってはならない。伝わらない語は用語集への登録に留めず、意味内容を表す語へ置き換える。

## 生成物の責務配置

### プロンプト階層定義

1. 共通 AGENTS.md (.chezmoitemplates/opencode/AGENTS.md)
   - 親エージェントと、親エージェントから作業票を受け取る汎用サブエージェントに適用される共通規則。
   - 可能な限り小さく抑える。
   - 分岐を書いてはいけない。Build エージェントの場合は～などと書きたくなったら、それは書くべき階層が異なる。
2. エージェント用システムプロンプト (dot_config/opencode/agents/\*.md, dot_config/opencode/agents/\*.md.tmpl)
   - Build、Plan、サブエージェント、コマンド用エージェントに対応したシステムプロンプト。
   - 要求の分類やエージェント固有の規則を書く。
   - 具体的な作業内容については言及してはいけない。スキルへの誘導はよい。
   - コマンド用エージェントは、スラッシュコマンドの `agent` から直接起動し、親エージェントの委譲判断と assignment packet（作業票）を通さない。
   - OpenCodeのコマンド本文は実行時プロンプトとして会話へ展開される。長い指示でTUIの会話履歴を埋めないため、コマンド本文は短い入口に留め、完全な実行指示をコマンド用エージェントのシステムプロンプトへ置く。
   - コマンド用エージェントには、共通 AGENTS.md とサブエージェント契約テンプレートを機械的に取り込まない。必要な規則は専用のシステムプロンプトへ閉じる。
3. 親エージェント用の委譲調整テンプレート (.chezmoitemplates/opencode/parent/\*.md)
   - 親エージェントがサブエージェントへ委譲するときの判断、並列化条件、作業票を書く。
   - サブエージェントプロンプトには展開しない。
4. サブエージェント契約テンプレート (.chezmoitemplates/opencode/agents/\*.md)
   - サブエージェントが受け取った作業票をどう解釈し、どの形式で返すかを書く。
   - 親エージェント側の並列委譲調整は書かない。
5. スキルの `description` (dot_agents/skills/\*/SKILL.md または SKILL.md.tmpl のフロントマターに書く `description` フィールド)
   - 1024文字以内で書く。
   - 日本語で、使う条件、使わない条件、得られるものが判断できるように書く。
   - `description` だけで、使う条件、使わない条件、得られるものが判断できるように書く。
   - 近いスキルと競合しやすい境界は、`description` の中で明示する。
   - 詳細手順、固定スキーマ、長いチェックリストは書かない。
6. スキル本文 (dot_agents/skills/\*/SKILL.md または SKILL.md.tmpl の本文)
   - `description` が決めた用途を前提に、手順、出力契約、検査、局所判断規則を具体的に書く。
   - `description` に書いた使用条件を重ね書きしない。
   - 日本語で短く書く。丁寧語を避け、仕様書的な短文と箇条書きを優先する。
   - 機械的に照合される制御語彙、フィールド名、スキル名、出力スキーマ名は英語のまま維持する。

### テンプレート展開と全体構成

このリポジトリは chezmoi のソースツリーである。プロンプト面は、ソース上の配置と展開先の対応で決まる。

### 展開の仕組み

- `dot_config/opencode/**` は `~/.config/opencode/**` へ、`dot_agents/skills/**` は `~/.agents/skills/**` へ展開される。
- `.tmpl` 拡張子のファイルはテンプレートとしてレンダリングされ、拡張子を落として展開される。プレーンな `.md` はそのままコピーされる。
- `.chezmoitemplates/opencode/**` は展開されない。テンプレート断片の置き場であり、次の2経路から参照される。
  - `{{ template "opencode/XXX" . }}`: 名前付きテンプレート参照。名前は `.chezmoitemplates/` からの相対パス（拡張子つき）。エージェント定義の合成に使う。
  - `{{ include ".chezmoitemplates/opencode/XXX.md" }}`: ソースルート相対パスで断片を直接取り込む。スキル本文が共有出力形式や共通完了チェックを参照するのに使う。
- テンプレートデータ（`.opencode`、`.opencode_agent` など）は `.chezmoidata/` のデータファイルから供給される。

### 展開先の一覧

| ソース                          | 展開先                  | 内容                                                                                           |
| ------------------------------- | ----------------------- | ---------------------------------------------------------------------------------------------- |
| `AGENTS.md`                     | 展開しない              | このファイル。プロンプト運用規則。このリポジトリをワークスペースにするとき毎セッション読まれる |
| `dot_config/opencode/**`        | `~/.config/opencode/**` | opencode の設定、エージェント定義、コマンド定義                                                |
| `dot_agents/skills/**`          | `~/.agents/skills/**`   | 共有スキル（SKILL.md と参照本文）                                                              |
| `.chezmoitemplates/opencode/**` | 展開しない              | テンプレート断片（合成と include の正本）                                                      |
| `.chezmoitemplates/prompt/**`   | 展開しない              | ハーネスをまたいで共有するプロンプト断片の正本                                                 |
| `.opencode/**`                  | 展開しない              | opencode の実行時データ（コマンド、エージェント、作業記録、失敗ログ）                          |
| `opencode-prompt-dev/**`        | 展開しない              | 運用文書と検査スクリプト                                                                       |

### エージェント定義の合成

- 親エージェントの定義（`dot_config/opencode/agents/build.md.tmpl`）は、次の断片を順に合成する。展開先は `~/.config/opencode/agents/build.md`。
  1. 共通規則（`opencode/AGENTS.md`）
  2. 親エージェント規則（`parent/parent.md`）
  3. 状態同期（`state-sync.md`）
  4. 委譲調整（`parent/delegation-orchestration.md`）
  5. 入力契約（`parent/subagent-assignment-packet.md`）
  6. 専用エージェントの入力契約（`agents/requirement-reviewer-input.md`、`agents/review-audit-input.md`）
- 計画エージェントの定義（`plan2.md.tmpl`）は、役割本文（`agents/plan.md`）に加えて共通規則と親エージェント規則の各断片を合成する。
- 汎用サブエージェントの定義（`general-fast` / `general-strong`）は、子の作業規則（`agents/general.md`）と共通規則の2断片だけを合成する。親エージェントの委譲調整は含めない。
- コマンド用エージェントはプレーンな `.md` のまま展開され、共通断片を取り込まない。スラッシュコマンドの `agent` から直接起動される。
- スキルの定義（`dot_agents/skills/<名前>/SKILL.md.tmpl`）はフロントマターと本文を持ち、共有断片を include する。参照本文（`references/` / `concerns/` / `profiles/`）はそのままコピーされる。

### 実行時に読まれるファイル

- opencode は起動時に、グローバル規則（`~/.config/opencode/AGENTS.md`）、ワークスペースの `AGENTS.md`（このリポジトリのルート）、選択されたエージェント定義（`~/.config/opencode/agents/<名前>.md`）を読み込む。
- `~/.config/opencode/AGENTS.md` はこのリポジトリに正本がないローカル追加である。話し方のペルソナなど、環境ごとの内容を載せる。

### 検証と適用

- レンダリング確認: chezmoi execute-template に断片またはテンプレート本文を渡して出力を確認する。
- 語彙整合: python opencode-prompt-dev/check_vocabulary.py を実行し、unaccounted / dangling / dead / file-ref が 0 であることを確認する。
- 展開差分: chezmoi diff --no-pager で確認する。
