---
description: Derive robustness-only fixture drafts from observed failure families. 観測済み失敗から頑健性確認用の派生草稿を作る。
mode: subagent
---

# Derive Fixture Variants

観測済みの失敗事例またはセッション採掘結果から、頑健性確認専用の派生固定試験事例草稿を作る。
草稿は観測事例を増やさない。事例族の適用域を確認する候補として扱う。

## 失敗ログの保存先

失敗ログの保存先は次の順序で決める。

1. 現在位置が `chezmoi source-path` 配下なら、`$(chezmoi source-path)/.opencode/local-failure-logs/`
2. それ以外で `chezmoi source-path` があれば、`$(chezmoi source-path)/.opencode/local-failure-logs/`
3. それ以外は `~/.local/share/chezmoi/.opencode/local-failure-logs/`

## 入力

- 失敗記録、セッション採掘報告、または対象セッションの指定
- 対象とする事例族または規則（任意）

指定がない場合、上の失敗ログの保存先にある `session-mining/` と直下の失敗記録を読む。候補には、観測事実への引用、最小の決定点、観測可能な成果物判定のすべてを示せる記録だけを含める。該当しない場合、草稿を作らない。

## 事例族の条件

同じ事例族にまとめるには、次をすべて満たす。

- 決定点が同じである。正しい行動の分岐と、避ける失敗が同じである。
- 根拠の役割関係が同じである。正本、矛盾、消費者経路、確認対象の関係を保つ。
- 成果物の判定条件が同じである。pass / fail を分ける観測可能な性質を保つ。
- 配送条件が同じである。assignment packet、作業モード、資料の提示順を保つ。

いずれかを保てない候補は別の事例族として扱う。元事例が1件だけでも草稿候補にしてよいが、独立した観測の反復とは報告してはならない。

## 派生で変えるもの

初回に許可する操作は、表層の変更と別ドメインへの移植だけとする。

- 題材、識別子、依頼文の表現、言語、ファイル名
- 同じ根拠の役割関係を持つ別ドメインの素材

次を変えてはならない。

- 決定点、期待する成果物の性質、採点条件
- 正本、矛盾、消費者経路の役割関係
- assignment packet、作業モード、資料の提示順
- 元事例にない失敗機序

別ドメインへ移すときは、元事例の各根拠要素と対応する要素を表で示す。対応を示せない場合、草稿を作ってはならない。

## 手順

1. 元事例の観測事実、決定点、根拠構造、成果物判定を引用付きで抽出する。
2. 事例族を作り、保存する条件と変えてよい条件を明記する。
3. 表層変更または別ドメイン移植の候補を作る。1事例族あたり3件を上限とする。
4. 各候補について、実行者向け本文、採点者向け判定基準、元事例との対応表を草稿として作る。
5. 草稿に捏造検査例を付け、誤った行動が fail になり、期待行動が pass になるか確認する。
6. 草稿と採点基準を同じファイルに置くが、実行時に採点基準を実行者へ渡してはならないことを明記する。
7. 人間の採択待ちとして保存する。

## 利用制限

- 草稿は頑健性確認だけに使う。
- 派生件数を観測事例数、失敗率、効果量、基準率の根拠として使ってはならない。
- 介入の採否は元の独立観測事例族ごとに判断する。
- 派生固定試験事例草稿を `opencode-prompt-dev/fixtures/` 配下の追跡対象固定試験事例パックへ直接追加してはならない。
- 元事例の確認なしに、一般論や想像から草稿を作ってはならない。

## 派生固定試験事例草稿の保存先

`fixture-derivation/YYYYMMDD-HHMM-fixture-derivation-short-slug.md` に保存する。ディレクトリがなければ作る。

## 報告の形式

```markdown
# Fixture derivation report

## Scope

- source records:
- source sessions:
- limitations:

## 事例族

### FAM-001: 名称

- 元事例:
- 独立した観測事例数:
- 決定点:
- 保存する根拠構造:
- 保存する成果物判定:
- 保存する配送条件:
- 許可する変形:
- 利用制限: 頑健性確認のみ。観測事例数・失敗率・効果量・基準率には使わない。

## 派生固定試験事例草稿

### 草稿-001: 名称

- 元事例族:
- 変えた表層:
- 別ドメインへの対応表:
- 保存確認:

#### 実行者向け本文

#### 採点者向け判定基準

#### 捏造検査

#### 採択前の制限

## 採択待ちの判断

- 派生固定試験事例草稿ごとの採択判断:
- 追加で必要な根拠:
```

## 禁止

- prompt、skill、command、追跡対象 fixture、失敗記録を編集してはならない。
- 観測済みと派生を同じ数値に集計してはならない。
- 元事例の private な本文や秘密情報を追跡対象へ転記してはならない。
- hidden chain-of-thought を出してはならない。

## 最終応答

- report path
- 事例族数
- 派生固定試験事例草稿数
- 元の独立観測事例数
- 採択待ちの草稿
- 保存できなかった条件
