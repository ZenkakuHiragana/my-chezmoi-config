---
name: context-clarification
description: 依頼から目的、対象、範囲、保つ条件、受け入れ結果、判断境界を実行前に明示する必要があるときに使う。観測事実と認可された判断から最小限に整理し、推測を要求へ変えず、未決を形式で閉じず、実行可能な合意または不足する判断を返す。
---

# 文脈整理

依頼を実行可能な最小の合意へ整理する。観測した事実、依頼者または既存の権威が認めた拘束、まだ決められない内容を分け、実装上の好みや推測を要求へ昇格させない。

## 確認するもの

- 依頼の目的と、依頼者が求める結果
- 変更または検討する対象と範囲
- 変えてよいもの、保つ意味、受け入れ結果
- 対象の正本、既存の仕様・取り決め、利用経路、確認できる観測
- 未決の問い、その判断を持つ主体、決めない場合の影響
- 実行者へ残してよい下位実装の選択

必要な事実の根拠が不足している場合は、未確認の事実、必要な照合内容、再開条件を返す。資料や実行結果への接触を推測で開始してはならず、確認できない事実は未確認のまま残す。

## 手順

1. 依頼から目的、成果、対象、範囲、保つ条件、受け入れ結果を読み取る。書かれていないものを補わない。
2. 観測事実、認可された拘束、仮説、未決の判断を分ける。現在の実装、テスト、設定、実行結果、一般論だけで新しい要求を作ってはならない。
3. 具体的な事実を使う場合は、所有先または提示された根拠へ直接対応付ける。提示された根拠がない場合は、照合が必要な未決として返す。根拠のない要約やモデルの好みを事実として扱ってはならない。
4. 複数の選択肢で受け入れ結果、公開インターフェイス、永続状態の所有先、結果を生む実経路、確認結果の意味が変わる場合は、その選択を持つ主体へ質問する。選択を内部で閉じてはならない。
5. 判断が必要な質問には、何を決めるのか、決めないと何が変わるのか、選択肢が結果へどう影響するのかを示す。質問は不足した判断を確認するためだけに行い、調査不足を新しい要求へ変えない。
6. その時点で既に認可された拘束だけを、実行者が使える最小の合意へ整理する。達成方法、具体的な探索、下位実装の選択を、明示要求がない限り拘束しない。
7. 合意から実行可能な結果、保つ条件、確認可能な受け入れ結果を読み直す。不足する判断、根拠、対象、範囲、結果があれば、推測で閉じず不足の所有者と再開条件を返す。

## 合意に含める内容

結果には、求める成果、対象範囲、含めない範囲、保つ意味、認可された制約、観測事実とその根拠、確認可能な受け入れ結果、未決の判断と判断主体、実行者へ委ねる選択を含める。形式は固定しない。

観測事実を規範的な要求として書き換えてはならない。既存の正本や取り決めが所有する実装詳細を、依頼者の要求として複製してはならない。

## 停止条件

- 必要な判断を持つ主体が選択を確定するまで、実行可能な合意として閉じてはならない。
- 根拠へ到達できない場合は、確認できなかった範囲と再開条件を返す。
- 受け入れ結果、公開インターフェイス、永続状態の所有先、実経路、確認結果の意味を変える未決を残したまま、実行開始を認めてはならない。
- 合意を作るために新しい構造、条件、識別子、固定形式を追加してはならない。

## 使わない場面

- 事実だけを資料や実行結果へ照合する作業
- 設計案そのものを比較する相談
- 固定済みの文章、コード、指示を検査するレビュー
- 必要な結果と保つ条件が既に確定しており、下位実装だけを行う作業

## 完了条件

- 目的、成果、対象、範囲、保つ条件、受け入れ結果を分けて確認した。
- 観測事実、認可された拘束、仮説、未決を混同していない。
- 根拠のない要求、推測による選択、未確認事項の形式的な解消がない。
- 判断が必要な選択を判断主体へ返した。
- 実行者が任された下位実装と、依頼者へ戻すべき判断を分けた。
- 不足時に停止し、再開条件を示した。
