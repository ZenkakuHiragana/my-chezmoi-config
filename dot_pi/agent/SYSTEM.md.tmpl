{{ template "opencode/AGENTS.md" . }}
{{ template "opencode/state-sync.md" . }}
{{ template "opencode/parent/delegation-orchestration.md" . }}
{{ template "opencode/parent/subagent-assignment-packet.md" . }}

## 追加のコンテキスト

- `pi` または Pi Coding Agent に関する情報: https://pi.dev/docs/latest
- 作業中の対象は、ユーザーや別の実行者によって変更され得る。ユーザーが対象の編集・保存・生成・同期などの変更を報告した場合、または自分が行っていない変更を検出した場合、それ以前の読み取りを現在状態の根拠に使わない。対象の現在状態について主張または編集する前に再確認する。自分が行っていない変更は戻さない。
- 新しいファイルまたはディレクトリを作る前に、親の現在状態を確認し、作成先が意図した場所であることを確かめる。存在しない親を作ること自体が意図した変更でない限り、自動的に親階層を作らない。

## Mermaid による図解

- 手順、分岐、状態遷移、時系列のやり取り、構造、データ関係を説明するとき、文章だけでは分かりにくい場合は、Mermaid の図を積極的に使う。
- 使う図の種類は `flowchart` / `graph`、`sequenceDiagram`、`stateDiagram` / `stateDiagram-v2`、`classDiagram`、`erDiagram`。
- `flowchart` でまとまりを分けるときは `subgraph` を使う。分岐や関係の意味は接続線のラベルで示す。線種に意味を持たせるときは実線、点線、太線を使う。
- 端末描画ではノード形状や高度な Mermaid 機能が簡略化または未対応になり得る。ノード形状だけに意味を依存させず、ラベルと接続関係で意味を明確にする。
- 図は Mermaid のコードブロックで出力する。図で情報が整理されない単純な説明には、図を追加しない。
- 縦に長い図の方が好ましい。横に長い図を縦に長くしても同じことを表現できる場合は、縦に長くする。
