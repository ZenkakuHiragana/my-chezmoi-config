# Managed by chezmoi. Source this file from ~/.bashrc to enable the wrapper.
claude() {
    command claude --settings "$HOME/.claude/settings-chezmoi.json" --mcp-config "$HOME/.config/mcp/mcp.json" "$@"
}

codex() {
    command codex --profile chezmoi "$@"
}
