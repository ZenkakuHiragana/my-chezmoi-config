# レビュー対象: 要件契約候補（skill-kb 設定表示の修正）

## 依頼引用

1. 「`get_source` が設定した情報源を全部返すようにしてほしい。いま6個設定してあるのに一部しか出ない」
2. 「ついでに、コードは将来も読みやすく保守しやすくしておいてほしい」

## 意味上の義務

| ID | 明示要求の逐語引用または認可元 | 必要な結果 | 対象範囲 | 保つ条件 | 結果を生む実経路 | 観測する結果 | 利用者または情報所有先 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| OBL-1 | 引用1 | `get_source` が設定済みの全情報源を返す | src/tools/get-source.ts | 情報源の並び順の既存仕様を変えない | MCP クライアントからの `get_source` 呼び出し | 設定に6情報源を置いたワークスペースで `get_source` が6件すべてを返す | MCP クライアント利用者 |
| OBL-2 | 引用2 | コードが将来も読みやすく保守しやすい状態である | src/ 全体 | なし | なし | 該当なし | 将来の保守者 |

## 判断と認可

| ID | 対応する義務ID | 判断 | 役割 | 確定状態 | 固定内容または認可範囲 | 認可元と根拠 | 適用範囲 | 戻り条件 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| D1 | OBL-1 | 返却順 | 制約 | 確認済み | 既存の並び順を維持する | 保つ条件の明文化 | OBL-1 | なし |

## 範囲

- 含む: src/tools/get-source.ts
- 含まない: 設定スキーマの変更

# 作業

渡された規則文書 `rules/review-rules.md` と `rules/review-discipline.md` の手順に従い、この要件契約候補を候補レビューとして検査せよ（各意味上の義務と契約全体）。受け入れテストは実行しない。出力は規則文書の出力形式に従う。
