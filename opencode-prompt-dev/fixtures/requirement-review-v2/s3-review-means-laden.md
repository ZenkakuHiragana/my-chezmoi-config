# レビュー対象: 要件契約候補（設定ローダーの堅牢化）

## 依頼引用

1. 「不正な設定ファイルで起動したら、黙って続行せずに止まってほしい」
2. 「README に設定例がほしい」

## 意味上の義務

| ID | 明示要求の逐語引用または認可元 | 必要な結果 | 対象範囲 | 保つ条件 | 結果を生む実経路 | 観測する結果 | 利用者または情報所有先 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| OBL-1 | 引用1 | 設定ローダーが不正な YAML を拒否して停止する | src/config-loader.ts | 正常な設定の読込挙動を変えない | CLI 起動時の設定読込 | `node dist/cli.js --config broken.yml` を実行し、stderr が正規表現 `^Error: invalid config` に一致し、終了コードが 1 である。実行は実装者とは別の実行者が Node.js 22 で行う。あわせて `wc -l src/config-loader.ts` で 200 行以内であることを確認する | CLI 利用者 |
| OBL-2 | 引用2 | README に設定例を1つ載せる | README.md | 既存節の構成を変えない | README の閲覧 | `grep -c '\`\`\`yaml' README.md` が 1 以上を返す。grep は GNU grep 3.8 を使う | README 読者 |

## 判断と認可

| ID | 対応する義務ID | 判断 | 役割 | 確定状態 | 固定内容または認可範囲 | 認可元と根拠 | 適用範囲 | 戻り条件 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| D1 | OBL-1 | 停止の形 | 制約 | 確認済み | 非零終了で停止する | 引用1 | OBL-1 | なし |

## 範囲

- 含む: src/config-loader.ts、README.md
- 含まない: 設定スキーマの変更

# 作業

渡された規則文書 `rules/review-rules.md` と `rules/review-discipline.md` の手順に従い、この要件契約候補を候補レビューとして検査せよ（各意味上の義務と契約全体）。受け入れテストは実行しない。出力は規則文書の出力形式に従う。
