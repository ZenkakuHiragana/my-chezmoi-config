# レビュー対象: 要件契約候補（帳票エクスポートの Windows 対応）

## 準備完了記録

- 文脈層: 依頼、サブシステム、ワークスペースは `confirmed`。外部基盤は `not_needed`
- 未解決の不足: なし
- 残る `user_decision`: なし
- 発見境界: 基準化、判断、実現、実行、評価は全て `no-decision`
- 要件契約候補: このファイルの依頼引用、意味上の義務、判断と認可、範囲
- 要件レビュー結果: 未実施
- 判定結果: レビュー完了まで `不合格`
- 仮定: なし
- 戻り先: `requirement-reviewer`
- 担当検査ID: `RR-OBL-1`、`RR-OBL-2`、`RR-CONTRACT-1`

## 依頼引用

1. 「帳票エクスポートを Windows 実機で動くようにしてほしい。WSL での代替実行は確認にならないので不可」
2. 「エクスポート処理は速くしておいてほしい」

## 意味上の義務

| ID    | 明示要求の逐語引用または認可元 | 必要な結果                                | 対象範囲    | 保つ条件                 | 結果を生む実経路                               | 観測する結果                                                                                                                                                                                                                     | 利用者または情報所有先 |
| ----- | ------------------------------ | ----------------------------------------- | ----------- | ------------------------ | ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- |
| OBL-1 | 引用1                          | 帳票エクスポートが Windows 実機で成功する | src/export/ | 既存の出力形式を変えない | Windows 実機のアプリ本体からのエクスポート操作 | Windows 実機でエクスポートを実行し、出力ファイルが生成される。確認には `/mnt/c/Program Files/dotnet/dotnet.exe` を使い、PowerShell 7.4.1 で `Get-FileHash -Algorithm SHA256` を実行して前回値と比較し、`ls -la` で日付を確認する | アプリ利用者           |
| OBL-2 | 引用2                          | エクスポート処理が高速である              | src/export/ | なし                     | エクスポート操作                               | 該当なし                                                                                                                                                                                                                         | アプリ利用者           |

## 判断と認可

| ID  | 対応する義務ID | 判断     | 役割 | 確定状態 | 固定内容または認可範囲     | 認可元と根拠 | 適用範囲 | 戻り条件 |
| --- | -------------- | -------- | ---- | -------- | -------------------------- | ------------ | -------- | -------- |
| D1  | OBL-1          | 確認環境 | 制約 | 確認済み | Windows 実機。WSL 代替不可 | 引用1        | OBL-1    | なし     |

## 範囲

- 含む: src/export/
- 含まない: 帳票のレイアウト変更

## 既存所有先

- `src/export/` のエクスポート実装はエクスポート機能が所有する。
- 出力形式は既存のエクスポートテストが所有する。
- Windows 実機、PowerShell 7.4.1、`/mnt/c`、`Get-FileHash`、`ls -la` は外部の実行環境であり、リポジトリ内の所有先は `該当なし`。
- エクスポート処理の速度を判定する既存の検査と所有先は `該当なし`。

# 作業

渡された規則文書 `rules/review-rules.md` と `rules/review-discipline.md` の手順に従い、この要件契約候補を候補レビューとして検査せよ（各意味上の義務と契約全体）。受け入れテストは実行しない。出力は規則文書の出力形式に従う。
