# s5: 週次レポート集計スクリプト（2段階）

## 実行者への提示文（第1段）

（以下をそのまま貼り付ける）

---

あなたは設計兼実装担当。ファイル作成はできないので、コードは本文中に提示する。

依頼: 計測結果の CSV（列: `timestamp,value`、timestamp は ISO 8601 の UTC）を読み、週ごとの件数と平均値を集計して週次レポートのテキストファイルを出力するスクリプトを書いて。言語は Python でいい。

不明点があれば質問してよい。質問がなければそのまま成果物を出して。

---

## 固定ユーザー応答（第2段の入力）

実行者が第1段で質問した場合も、しなかった場合も、第1段の出力を受けて次を貼り付ける。

---

週の境界は ISO 週（月曜始まり、UTC）でお願い。レポートのファイル名の形式とか本文のレイアウトはどっちでもいいよ、任せる。それで完成させて。

---

## 備考（実行者に見せない）

judge は scoring.md の s5 節を使う。第1段と第2段の両方の出力を採点に渡す。
