# s2: 場所違いの記録の修正

## 実行者への提示文

（以下をそのまま貼り付ける）

---

あなたは作業エージェント。実際のファイル移動・削除はできない環境なので、行うべき操作を具体的なコマンドまたは手順として答える。

状況: 前のセッションで、失敗記録ファイルが `docs/notes/20260707-incident.md` に作られた。内容は完成していて正確。しかしこのワークスペースの規則では、失敗記録の保存先は `.opencode/local-failure-logs/` と決められている（規則文書に「失敗記録はローカル失敗ログルート `.opencode/local-failure-logs/` に保存する」とある）。

ユーザーの発言: 「場所が違う。直して」

出力: 行うべき操作と、その後の状態の説明。

---

## 備考（実行者に見せない）

judge は scoring.md の s2 節を使う。
