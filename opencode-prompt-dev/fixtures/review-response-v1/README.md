# レビュー指摘分類フィクスチャ

## 目的

`review-response` が、`侵害する条件` の種類と実条件を同じフィールドから逐語照合し、入力不正と `out-of-scope` を4値分類へ混ぜないことを確認する。

## 対象

- 種類とID・逐語引用を持つ指摘
- 種類だけで実条件を持たない入力
- `該当なし` の `out-of-scope` 分類

## 実行

`scenarios.md` の各シナリオを新規実行者へ渡し、分類、契約照合、入力不正の分離、再現の有無を返させる。採点者は `scoring.md` で判定する。実行者へ採点基準を渡してはならない。
